<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <%= link_to "CatStore", root_path, class: "navbar-brand" %>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button> 
  
    <div class="collapse navbar-collapse" id="navbarColor01">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item">
          <%= link_to "Accueil", root_path, class: "nav-link nav-link-hover" %>
        </li>
          <% if user_signed_in?  %>
          <li class="nav-item">
            <%= link_to "Deconnexion",destroy_user_session_path, method: :delete, class: "nav-link nav-link-hover" %>
          </li>
        <% else %>
        <li class="nav-item">
          <%= link_to "Connexion", user_session_path, class: "nav-link nav-link-hover" %>
        </li>
        <li class="nav-item">
            <%= link_to "Inscription", new_user_registration_path, class: "nav-link nav-link-hover" %>
          </li>
          <% end %>
        </ul>
    </div>

          <div class="navbar-collapse collapse ">
        <ul class="navbar-nav ml-auto">
          <li class="nav-item">
         </li>
         <% if user_signed_in?  %>
          <li><%= link_to "Profil", user_path(current_user.id), class:"btn btn-primary" %>
          </li>
          <% end %> 
         <% if user_signed_in?  %>
          <li><%= link_to "Panier", cart_path(current_user.id), class:"btn btn-primary" %>
          </li>
          <% end %> 
        </ul>
  </nav>
  